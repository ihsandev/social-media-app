export const baseUrl = 'https://jsonplaceholder.typicode.com'

const API = {
  users: `${baseUrl}/users`,
  posts: `${baseUrl}/posts`,
  albums: `${baseUrl}/albums`,
  photos: `${baseUrl}/photos`
}

export default API;